// Development configuration flags
export const DEV_CONFIG = {
  // Set to true to bypass backend email verification
  // This will skip the email verification step completely during registration
  SKIP_EMAIL_VERIFICATION: true
};
